// This tab is never actually shown — its tabPress listener (see (tabs)/_layout.tsx) intercepts
// the press and opens the /transactions/add modal instead. The route file just needs to exist.
export default function AddPlaceholder() {
  return null;
}
